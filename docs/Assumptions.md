# STE v0.1 — Assumptions (bootstrap)
- Node LTS 22.x; pnpm 9.x (pinned).
- Monorepo: apps/web (Next.js), apps/api (NestJS).
- Branch protection: temporal con ruleset no-enforced; disciplina PRs (riesgo aceptado).
- Próximo: Postgres (Docker), .env tipado, CI gates, Renovate.
